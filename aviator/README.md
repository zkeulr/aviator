# README

## Connect:

To connect, run

```sh
screen -L $(ls /dev/ttyACM* | head -1)
```

To disconnect, press `CTRL+A` then `CTRL+D`.

## Configure:

Create `settings.toml` and fill in:

```toml

```
