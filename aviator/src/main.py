# on boot
# initialize network from saved settings, set else
# initialize display
# initialize location from saved settings, set else
import time
import adsb

data = {}
    

def main():
    
    while(True):
      time.sleep(60)
    
      # Fetch ADS-B data

      # Fetch API data

if __name__ == "main":
    main()